AWSTemplateFormatVersion: '2010-09-09'

Parameters:
  Project:
    Type: String
  ProjectLower:
    Type: String
  Environment:
    Type: String

Resources:
  OrganizerEventsTable:
    Type: 'AWS::DynamoDB::Table'
    Properties:
      TableName: !Sub '${Project}-ORGANIZER-EVENT-BUNDLE-${Environment}'
      BillingMode: 'PAY_PER_REQUEST'
      AttributeDefinitions:
        - AttributeName: "organizerId"
          AttributeType: "S"
        - AttributeName: "month"
          AttributeType: "S"
      KeySchema:
        - AttributeName: "organizerId"
          KeyType: "HASH"
        - AttributeName: "month"
          KeyType: "RANGE"
      GlobalSecondaryIndexes:
        - IndexName: !Sub "monthIndex"
          KeySchema:
            - AttributeName: "month"
              KeyType: "HASH"
          Projection:
            ProjectionType: "KEYS_ONLY"

  ConfigurationTable:
    Type: 'AWS::DynamoDB::Table'
    Properties:
      TableName: !Sub '${Project}-CONFIGURATION-${Environment}'
      BillingMode: 'PAY_PER_REQUEST'
      AttributeDefinitions:
        - AttributeName: "id"
          AttributeType: "S"
      KeySchema:
        - AttributeName: "id"
          KeyType: "HASH"
